"use client";
{/*}
const Category = () => {
    const categoryList = [
        { name: "computer" },
        { name: "computer" },
        { name: "computer" },
        { name: "computer" },
        { name: "computer" },
        { name: "computer" }
        
    ];

    return (
        <div className="flex items-center justify-center px:3 md:px:10 gap-3 md:gap-10 md:py-10  overflow-x-auto">
            {categoryList.map((category, index) => (
                <div  className="border-2 border-gray-400 text-slate-500 rounded-full min-w-[120px] flex items-center justify-center cursor-pointer px-4 py-2 text-center shadow-md hover:border-blue-600 transition-all duration-300" key={index}>
                    {category.name}
                    </div>
            ))}
        </div>
    );
}

export default Category;

*/}